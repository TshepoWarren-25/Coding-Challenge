{
  "name": "flag-explorer-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "nodemon --watch src --exec \"deno run -A --unstable src/dev_server.ts\" || ts-node-dev --respawn src/main.ts",
    "build": "tsc -p tsconfig.json",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "ts-node": "^10.9.1",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.12",
    "supertest": "^7.0.0",
    "@types/supertest": "^2.0.12",
    "ts-jest": "^29.2.5",
    "nodemon": "^2.0.22"
  }
}